services:
  galochka-hedra-bot:
    build: .
    container_name: galochka-hedra-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    command: python -m app.main
